# 基于高效安全多方计算的隐私保护跌倒检测系统

**作者：丛语洛　学号：2022100994　指导教师：杨安家**

**暨南大学 网络空间安全学院 网络空间安全专业**

---

## 摘要

随着全球人口老龄化进程的加速，跌倒已成为威胁老年人生命健康的重大公共卫生问题。据统计，65岁以上老年人年均跌倒发生率介于28%至35%之间，而跌倒后的"长躺"现象可引发肺炎、深静脉血栓等严重并发症，显著增加死亡率。穿戴式惯性测量单元（Inertial Measurement Unit, IMU）结合云端智能分析的跌倒检测方案，因其部署便捷、成本低廉，已成为当前研究与产业应用的主流范式。然而，该范式将包含敏感生物特征信息的原始传感器数据上传至云端，存在严峻的隐私泄露风险——已有研究表明，基于加速度时序数据可高精度推断用户的步态模式、性别、身高、体重乃至地理位置等隐私属性。如何在保障检测精度的同时实现传感器数据的隐私保护，已成为医疗物联网领域亟待解决的关键科学问题。

本文针对上述问题，设计并实现了一种基于高效安全多方计算（Secure Multi-Party Computation, MPC）的隐私保护跌倒检测系统。本文的核心贡献在于：第一，将面向轻量级比较任务的高效MPC协议首次应用于跌倒检测场景，实现了专用密码学协议与信号处理算法的深度协同适配；第二，通过严格的数学等价变换，将明文两阶段阈值检测算法中的开方运算、除法运算及反三角函数运算完全消除，使其仅需安全乘法与安全比较两种基础MPC运算即可实现，显著降低了协议复杂度与通信开销；第三，构建了端-云协同的两方安全计算架构，在SisFall公开数据集上完成了端到端仿真实验验证。实验结果表明，本文提出的MPC隐私保护方案在50跌倒样本与50正常样本构成的测试集上取得了94.0%的准确率、94.0%的精确率、94.0%的召回率与0.940的F1值，相较于明文检测方案的96.0%精度，精度损失约为2%，满足医疗辅助检测系统对误差容忍度的设计要求。在延迟性能方面，单样本端到端检测延迟约为700 ms，10样本批量检测约为920 ms，50样本批量检测约为1780 ms，其中安全乘法运算占总延迟的64%至67%，安全比较运算占29%至31%，客户端预处理占约6%。本文的方案相较于现有基于三方架构的MPC跌倒检测研究，在通信轮数、架构复杂度与实现可行性方面均具有优势。

**关键词：** 安全多方计算；隐私保护；跌倒检测；算术秘密分享；Beaver三元组

---

## Abstract

With the acceleration of global population aging, falls have become a major public health threat to the elderly. The annual incidence of falls among individuals aged 65 and above ranges from 28% to 35%, and the "long lie" phenomenon after falls can trigger severe complications such as pneumonia and deep vein thrombosis, significantly increasing mortality. Wearable Inertial Measurement Unit (IMU) combined with cloud intelligent analysis has become the mainstream paradigm for fall detection due to its convenient deployment and low cost. However, this paradigm uploads raw sensor data containing sensitive biometric information to the cloud, posing severe privacy leakage risks. Existing studies have demonstrated that gait patterns, gender, height, weight, and even geographical location can be inferred with high accuracy from acceleration time-series data.

This thesis designs and implements a privacy-preserving fall detection system based on efficient Secure Multi-Party Computation (MPC). The core contributions are threefold. First, an efficient MPC protocol tailored for lightweight comparison tasks is applied to fall detection for the first time, achieving deep collaborative adaptation between specialized cryptographic protocols and signal processing algorithms. Second, through rigorous mathematical equivalence transformations, square root, division, and inverse trigonometric operations in the plaintext two-stage threshold detection algorithm are completely eliminated, reducing the protocol to only secure multiplication and secure comparison operations. Third, an end-cloud collaborative two-party secure computation architecture is constructed and validated through end-to-end simulation experiments on the public SisFall dataset. Experimental results demonstrate that the proposed MPC scheme achieves 94.0% accuracy, 94.0% precision, 94.0% recall, and 0.940 F1-score on a test set comprising 50 fall samples and 50 normal samples, with approximately 2% accuracy loss compared to the plaintext baseline, satisfying the design requirements. The end-to-end latency for single-sample detection is approximately 700 ms.

**Keywords:** Secure Multi-Party Computation; Privacy Preservation; Fall Detection; Arithmetic Secret Sharing; Beaver Triples

---

## 第1章 绪论

### 1.1 研究背景与意义

#### 1.1.1 老龄化社会与跌倒危害

全球人口老龄化已成为21世纪最显著的人口结构变迁趋势之一。根据世界卫生组织（World Health Organization, WHO）发布的报告，全球65岁及以上老年人口比例正在以史无前例的速度增长，预计到2050年将达到16%。在这一人口结构转型的背景下，老年人健康问题日益受到学界与产业界的广泛关注。其中，意外跌倒被公认为威胁老年人独立生活能力与生命健康的首要风险因素。流行病学统计数据显示，65岁以上社区老年人的年均跌倒发生率介于28%至35%之间，而80岁以上高龄人群的年均跌倒发生率可高达50%以上。更为严峻的是，跌倒并非仅造成即时的物理创伤——约50%的跌倒事件导致老年人无法自行起身，形成所谓的"长躺"（long lie）状态。若长躺时间超过1小时，患者发生吸入性肺炎、横纹肌溶解、深静脉血栓形成及低体温症等严重并发症的概率将急剧上升，死亡率可提高至未发生长躺情形的5倍以上。因此，构建高效、可靠的自动跌倒检测系统，对于缩短跌倒响应时间、降低并发症发生率、提升老年人生活质量具有至关重要的社会意义与医疗价值。

#### 1.1.2 穿戴式跌倒检测的技术矛盾

当前，基于穿戴式惯性测量单元的跌倒检测系统已展现出良好的应用前景。该类系统通过部署于腰部、手腕或胸前的微型IMU传感器，以50 Hz至200 Hz的采样频率持续采集三轴加速度数据，并借助阈值判定算法或机器学习模型实现跌倒事件的自动识别。得益于微机电系统（Micro-Electro-Mechanical Systems, MEMS）技术的成熟，现代加速度计已可在极低功耗下实现高精度运动感知，为穿戴式检测提供了坚实的硬件基础。在系统架构层面，"端-云协同"模式已成为行业主流：传感器端负责数据采集与初步预处理，云端服务器承担计算密集型的检测算法推理任务。该模式有效缓解了终端设备的计算资源约束，同时便于算法的远程迭代更新与多用户管理。

然而，上述"端-云协同"架构在带来计算便利性的同时，也引入了一个根本性的隐私安全矛盾：原始加速度时序数据必须从用户终端传输至云端服务器。加速度数据属于高度敏感的个人生物特征信息——研究表明，基于仅需0.3秒的加速度片段，攻击者即可实现100%准确率的性别识别；基于步态频谱特征，可高精度推断用户的身高、体重与年龄；更为严重的是，结合地理位置信息，加速度数据可用于追踪用户的日常活动轨迹与生活习惯。在《中华人民共和国个人信息保护法》将生物识别信息列为敏感个人信息的法律框架下，将未加保护的原始IMU数据上传至云端，不仅存在合规风险，更直接威胁用户的隐私安全。因此，如何在端-云协同的跌倒检测系统中实现"数据可用不可见"，即在保障云端检测能力的前提下确保原始传感器数据不被泄露，已成为该领域亟待解决的核心技术难题。

#### 1.1.3 现有隐私保护技术的局限性

针对穿戴式健康监测中的隐私保护需求，学术界已提出了多种技术路线，主要包括联邦学习（Federated Learning, FL）、同态加密（Fully Homomorphic Encryption, FHE）与可信执行环境（Trusted Execution Environment, TEE）。然而，上述方案在跌倒检测场景下均存在不同程度的局限性。

联邦学习通过将模型训练过程分布在各参与方本地，仅上传模型参数更新而非原始数据，在一定程度上降低了数据泄露风险。然而，联邦学习主要解决的是训练阶段的隐私保护问题，而跌倒检测的核心需求在于推理阶段——即如何在不暴露原始传感器数据的前提下完成实时检测。更为关键的是，已有研究证实，基于模型参数更新与辅助信息，攻击者可通过成员推理攻击（membership inference attack）与模型逆向攻击（model inversion attack）部分恢复训练数据中的敏感信息。因此，联邦学习无法从根本上解决跌倒检测推理阶段的原始数据隐私问题。

同态加密允许在密文域直接执行任意计算，理论上可实现完美的数据隐私保护。Gentry于2009年提出的基于理想格的全同态加密方案，奠定了该领域的理论基础。然而，同态加密的计算开销极为庞大——即便是轻量级的Somewhat Homomorphic Encryption方案，其单比特乘法运算的开销亦比明文运算高出4至6个数量级。在实时跌倒检测场景下，每秒100次以上的传感器采样要求系统具备毫秒级的响应延迟，而同态加密方案的典型延迟往往以秒甚至分钟计量，难以满足实时性要求。

可信执行环境依赖硬件级别的安全隔离机制（如Intel SGX、ARM TrustZone）来保护敏感计算。尽管TEE在性能方面具有优势，但其安全性完全依赖于特定硬件供应商的实现正确性与侧信道攻击防护能力。近年来，针对Intel SGX的Spectre类攻击、Plundervolt电压故障注入攻击等安全事件，暴露了TEE在对抗复杂物理攻击时的脆弱性。此外，TEE方案要求云端服务器配备特定型号的CPU，限制了其在异构计算环境中的部署灵活性。

#### 1.1.4 安全多方计算的技术优势

安全多方计算作为一种密码学原语，允许多个参与方在不泄露各自私有输入的前提下，联合计算某一约定函数并获得正确结果。MPC的安全性不依赖于任何硬件假设或可信第三方，而是基于严格的数学困难性假设与信息论安全论证，从根本上消除了单点信任风险。在跌倒检测的端-云协同架构中，MPC可实现以下安全目标：用户终端将原始传感器数据拆分为多个数学上不可区分的随机份额，分别发送至两台互不信任的云端服务器；服务器在份额域上执行安全计算协议，最终仅输出布尔型的跌倒判定结果。在整个过程中，任意单台服务器均无法从接收到的份额中还原出原始传感器数据，实现了"数据可用不可见"的隐私保护效果。

本文聚焦于两方安全计算架构，即由客户端（可穿戴设备）、云端服务器S1与云端服务器S2构成的三方交互模型。相较于传统的三方计算架构（要求三台服务器共同参与每一轮运算），两方架构将客户端纳入份额生成与结果重构环节，将云端服务器数量减少至两台，显著降低了通信轮数与系统部署复杂度。本文的研究目标在于：针对跌倒检测两阶段阈值算法的计算特性，设计专用的高效MPC协议，在保障检测精度的同时控制通信延迟于可接受范围内，为医疗物联网中的隐私保护健康监测提供可行的技术方案。

### 1.2 国内外研究现状

#### 1.2.1 跌倒检测算法研究进展

跌倒检测算法的研究可追溯到2000年代初期，其发展脉络大致经历了从阈值法到机器学习法、再到深度学习法的演进过程。阈值法作为最经典的检测范式，通过提取加速度信号的幅值、变化率等物理量，并与预设阈值进行比较以判定跌倒事件。Kangas等人于2008年在《Gait & Posture》期刊上发表的研究，系统比较了多种基于躯体附着式加速度计的低复杂度跌倒检测算法，验证了合加速度幅值（Signal Vector Magnitude, SVM）与速度变化量作为判别特征的有效性。Bourke等人于2007年提出的基于三轴加速度阈值判定的算法，通过冲击检测与姿态变化检测的级联结构，实现了对跌倒事件的高灵敏度识别。阈值法的核心优势在于计算复杂度极低、推理延迟短、模型可解释性强，非常适用于资源受限的嵌入式设备与实时检测场景。

随着机器学习技术的发展，研究者开始将支持向量机（Support Vector Machine, SVM）、随机森林（Random Forest, RF）、隐马尔可夫模型（Hidden Markov Model, HMM）等分类器应用于跌倒检测。相较于阈值法，机器学习方法通过自动学习特征与标签之间的非线性映射关系，在复杂场景下通常可获得更高的检测精度。Sadek等人于2019年提出的基于深度传感器骨骼数据的跌倒检测方法，利用人体骨架关键点序列实现了对跌倒动作的时空建模。然而，机器学习与深度学习方法通常需要大量的浮点矩阵运算、非线性激活函数计算与多层网络前向传播，这些运算在MPC密文域中的实现代价极高——例如，ReLU激活函数的安全计算需要逐元素的安全比较操作，而全连接层的矩阵乘法需要大量的安全乘法协议调用。因此，对于本文聚焦的MPC隐私保护场景，计算简洁的阈值法比复杂的深度学习模型具有天然的协议适配优势。

#### 1.2.2 隐私保护技术对比分析

在健康监测数据的隐私保护领域，联邦学习、同态加密、可信执行环境与安全多方计算构成了当前主流的四大技术路线。联邦学习的核心思想是"数据不动模型动"——各参与方在本地数据上训练模型，仅周期性地上传模型梯度或参数更新至中央服务器进行聚合。McMahan等人于2017年提出的联邦平均（FedAvg）算法，为分布式深度学习提供了高效的参数聚合方案。然而，如前所述，联邦学习无法保护推理阶段的原始数据，且模型更新本身可能泄露训练数据信息。

同态加密允许在加密数据上直接执行算术运算，计算结果解密后与明文运算结果一致。Gentry于2009年首次构造了基于理想格的全同态加密方案，证明了任意计算可在密文域实现的可行性。然而，全同态加密的启动成本（bootstrapping）与密文膨胀率（ciphertext expansion）使其在实时健康监测场景中难以实用化。部分同态加密方案（如Paillier加法同态加密）虽然计算效率较高，但仅支持加法运算或有限次数的乘法运算，难以满足跌倒检测算法中同时涉及乘法与比较运算的需求。

可信执行环境通过硬件级别的内存加密与访问控制，为敏感计算创建隔离的执行区域。Intel SGX技术允许应用程序在称为"安全飞地"（enclave）的受保护内存区域中运行，即使操作系统或hypervisor被攻破，飞地内的数据与代码仍然保密。然而，TEE面临着侧信道攻击、 Spectre类推测执行攻击与电压故障注入攻击等安全威胁，且其性能受限于安全上下文切换开销。

安全多方计算在理论上可实现不依赖任何可信第三方的安全联合计算。Yao于1986年提出的乱码电路（Garbled Circuits）协议与Goldreich、Micali和Wigderson于1987年提出的GMW协议，奠定了现代MPC的理论基础。Beaver于1991年提出的电路随机化技术，通过离线预生成乘法三元组将在线安全乘法的通信轮数降至1轮，显著提升了MPC的实用性。Demmler等人于2015年开发的ABY框架，支持在算术分享、布尔分享与乱码电路三种表示之间高效转换，成为MPC应用开发的重要工具。然而，通用MPC框架通常面向通用电路设计，未针对特定信号处理算法的计算模式进行优化，存在协议层与算法层脱节的问题。

#### 1.2.3 MPC在健康监测中的应用现状

MPC技术在医疗健康领域的应用近年来取得了显著进展。Cramer、Damgård和Nielsen于2015年出版的专著系统论述了安全多方计算与秘密分享的理论体系，为MPC在健康监测中的应用奠定了理论基础。Damgård等人于2012年提出的SPDZ协议，在恶意多数方模型下实现了高效的MPC计算，为需要强安全保证的医疗应用提供了协议支撑。

在跌倒检测这一具体应用场景中，Mainali与Shepherd于2019年发表的研究是当前唯一将MPC技术应用于跌倒检测的文献。该研究采用三方服务器架构，基于通用MPC框架实现了机器学习分类器的隐私保护推理，在最佳情况下实现了365.2 ms的检测延迟。然而，该方案存在以下不足：第一，三方架构要求三台服务器同时在线并参与每一轮通信，系统复杂度与故障容错成本较高；第二，基于通用MPC框架的实现未针对跌倒检测算法的计算特性进行专门优化，协议效率存在提升空间；第三，采用机器学习分类器而非阈值法，导致密文域计算开销较大。

综上所述，现有研究在跌倒检测的隐私保护方面仍存在以下空白：尚未有研究针对两阶段阈值跌倒检测算法的计算特性，设计专用的MPC协议适配方案；两方架构相较于三方架构的通信效率优势，在跌倒检测场景中尚未得到充分探索；算法层与密码学协议层的协同设计方法缺乏系统性研究。本文正是在上述背景下，致力于填补专用协议适配与两方架构优化方面的研究空白。

### 1.3 研究内容与主要贡献

本文围绕"如何在保障跌倒检测精度的前提下，利用安全多方计算技术实现穿戴式IMU数据的隐私保护"这一核心科学问题，展开了系统深入的研究。本文的研究内容与主要贡献可概括为以下三个方面：

**贡献一：将高效比较协议首次应用于跌倒检测场景，实现专用MPC协议的领域适配。** 本文深入分析了两阶段阈值跌倒检测算法的计算模式，识别出算法中的核心运算为乘法与比较操作。基于此，本文将面向轻量级比较任务的高效MPC比较协议适配至跌倒检测场景，避免了通用MPC框架中冗余的协议转换开销，实现了密码学协议与信号处理算法的深度协同。

**贡献二：通过严格的数学等价变换，消除明文算法中的MPC不友好运算。** 明文两阶段阈值算法涉及开方运算（SVM幅值计算）、除法运算（倾斜角计算）与反三角函数运算（arccos反余弦计算）。上述运算在MPC密文域中的实现代价极高。本文通过严格的数学推导，证明了上述运算可被等价的乘法与比较运算完全替代：冲击检测中的SVM幅值比较转化为SVM平方与阈值平方的比较；姿态检测中的倾斜角比较通过余弦函数的单调性与代数变形，转化为系数乘积与平方项的线性比较；最终的两阶段逻辑与通过{0,1}比特上的安全乘法实现。上述变换将协议复杂度降至仅依赖安全乘法与安全比较两种基础运算。

**贡献三：构建端-云协同两方架构并开展端到端实验验证。** 本文设计并实现了由客户端、云端服务器S1与云端服务器S2构成的两方安全计算系统，采用半诚实（semi-honest）安全模型。系统在SisFall公开数据集上进行了全面的性能评估，实验结果表明：MPC隐私保护方案相较于明文基线仅产生约2%的精度损失，满足医疗辅助检测系统的设计要求；单样本端到端检测延迟约为700 ms，在可接受范围内；系统在不同网络延迟条件下表现出良好的鲁棒性。

### 1.4 论文组织结构

本文共分为六章，各章节内容安排如下：

**第1章 绪论。** 阐述研究背景与意义，分析老龄化社会跌倒检测的迫切需求与现有端-云架构的隐私矛盾，对比联邦学习、同态加密、可信执行环境与安全多方计算四种隐私保护技术路线的优劣，明确本文的研究目标与创新点。

**第2章 相关技术基础。** 介绍跌倒检测的阈值法原理与SisFall数据集，系统阐述算术秘密分享、Beaver三元组、安全比较协议与半诚实安全模型的理论基础，为后续章节提供必要的密码学预备知识。

**第3章 隐私保护跌倒检测系统设计。** 提出端-云协同两方架构，详细设计客户端模块（数据采集、定点数编码、秘密分享）与云端服务器模块（Beaver三元组预生成、安全计算引擎），并对全生命周期的隐私保护机制进行安全性分析。

**第4章 两阶段跌倒检测算法及MPC安全改造。** 作为本文核心章节，首先给出明文两阶段阈值算法及其参数实验调优过程；然后通过严格的数学推导，给出冲击判定、姿态判定与逻辑与运算的MPC等价变换；最后用协议伪代码描述完整的MPC安全流程，并进行正确性与安全性分析。

**第5章 系统实现与性能评估。** 介绍实验环境与数据集，从正确性验证、精度对比、延迟分解、综合方案对比与网络鲁棒性五个维度开展实验评估，并对实验结果进行深度分析。

**第6章 总结与展望。** 总结本文的主要研究成果，分析现有方案的局限性，并对未来在恶意安全模型、嵌入式部署与复杂算法MPC适配等方向的研究进行展望。

---

## 第2章 相关技术基础

### 2.1 跌倒检测技术概述

#### 2.1.1 两阶段阈值法原理

跌倒是一种具有显著动力学特征的复杂人体运动事件。从生物力学角度分析，典型的跌倒过程可划分为三个连续阶段：失平衡阶段（loss of balance）、冲击阶段（impact）与静止阶段（lying still）。其中，冲击阶段是跌倒事件最具辨识度的特征窗口——人体躯干与地面发生剧烈碰撞，导致三轴加速度出现瞬时大幅跃升。基于上述物理特性，两阶段阈值检测算法将跌倒识别任务分解为"冲击检测"与"姿态检测"两个级联子任务：第一阶段通过监测合加速度幅值是否超过预设冲击阈值，判定是否发生剧烈碰撞；第二阶段通过监测人体躯干倾斜角是否超过预设姿态阈值，判定碰撞后人体是否处于水平躺卧姿态。仅当两个阶段均判定为阳性时，系统才最终输出跌倒警报。

合加速度幅值（Signal Vector Magnitude, SVM）定义为三轴加速度矢量模长的瞬时度量：

$$SVM = \sqrt{a_x^2 + a_y^2 + a_z^2}$$

其中 $a_x, a_y, a_z$ 分别表示传感器坐标系下 $x, y, z$ 三轴的加速度分量。SVM消除了传感器佩戴方向的不确定性，对跌倒检测具有旋转不变性的优良特性。当人体发生跌倒碰撞时，SVM通常在100 ms至300 ms的时间窗口内出现超过2$g$（约19.6 m/s²）的瞬时峰值，与日常活动（如行走、坐下、弯腰）的加速度模式形成显著区分。

倾斜角 $\theta$ 定义为人体躯干纵轴与重力方向的夹角，可通过加速度矢量在竖直方向的分量与SVM的比值计算：

$$\theta = \arccos\left(\frac{a_z}{SVM}\right)$$

正常站立状态下，$\theta \approx 0°$；完全水平躺卧状态下，$\theta \approx 90°$。通过设定合理的姿态角度阈值，可有效区分跌倒后的躺卧姿态与弯腰、蹲下等非跌倒低姿态活动。

#### 2.1.2 SisFall数据集介绍

SisFall数据集是由哥伦比亚安蒂奥基亚大学于2015年发布的一个公开跌倒检测数据集，专门针对基于加速度计的跌倒检测算法评估而构建。该数据集收集了38名志愿者（包括19名19至30岁的青年受试者与19名60至75岁的老年受试者）在腰部佩戴ADXL345三轴加速度计时的运动数据。数据集共包含4,505条记录，其中跌倒活动（Fall, Activities of Daily Living, ADL）2,115条，日常活动2,390条。跌倒活动涵盖了向前跌倒、向后跌倒、向侧方跌倒、因晕厥导致的瘫软跌倒等多种典型模式；日常活动则包括行走、慢跑、坐下、起立、弯腰拾物、上下楼梯等15类常见动作。

数据采样频率为200 Hz，每条记录包含时间戳与三轴加速度值（单位为$g$）。数据文件采用文本格式存储，便于直接读取与处理。SisFall数据集的优势在于：同时包含青年与老年受试者数据，考虑了年龄因素对运动模式的影响；跌倒与日常活动的类别划分细致，便于评估算法对混淆动作的区分能力；传感器佩戴位置统一（腰部），避免了佩戴位置差异带来的数据异质性。因此，SisFall数据集已成为跌倒检测领域广泛采用的基准数据集之一。本文选用SisFall数据集作为实验数据源，通过其公开的跌倒与日常活动样本，对本文提出的隐私保护跌倒检测方案进行全面的正确性与性能评估。

#### 2.1.3 本文选用阈值法的理由

本文选用两阶段阈值法作为明文检测基线，主要基于以下三方面的考量：

第一，**MPC协议适配性**。阈值法的核心运算为加法、乘法与比较操作，而这三种运算恰好对应MPC中最基础、最高效的安全协议。相比之下，机器学习分类器（如支持向量机、神经网络）通常涉及指数函数、对数函数、sigmoid激活函数等非线性运算，这些运算在MPC密文域中的实现需要复杂的近似协议或查表协议，计算开销与通信轮数均显著增加。因此，从密码学协议实现的角度出发，简洁的阈值法比复杂的机器学习模型更适合MPC隐私保护场景。

第二，**实时性需求**。跌倒检测系统需要在跌倒发生后尽可能短的时间内发出警报，以争取黄金救援时间。阈值法的计算流程极为简洁——仅需若干次乘加运算与两次比较运算，在明文环境下可在微秒量级完成推理。即使经过MPC安全改造后，其密文域计算延迟仍可控制在秒级范围内，满足实时性要求。

第三，**可解释性与可调试性**。阈值法的判定逻辑完全透明——每一个中间结果（如SVM幅值、倾斜角）均具有明确的物理意义，便于医学领域专家理解与验证。当检测出现误报或漏报时，可通过调整阈值参数直接干预系统行为，这种可解释性对于医疗辅助系统的临床落地至关重要。

### 2.2 安全多方计算基础

#### 2.2.1 算术秘密分享

算术秘密分享（Arithmetic Secret Sharing）是安全多方计算中最基础的数据表示方式之一。在本文聚焦的两方计算场景中，设秘密值 $x$ 属于整数环 $\mathbb{Z}_{2^{32}}$，则 $x$ 的秘密分享表示为一对随机份额 $[x]_0$ 与 $[x]_1$，满足：

$$[x]_0 + [x]_1 \equiv x \pmod{2^{32}}$$

其中 $[x]_0$ 由服务器S1持有，$[x]_1$ 由服务器S2持有。份额的生成过程如下：首先随机采样 $r \xleftarrow{\$} \mathbb{Z}_{2^{32}}$，然后计算 $[x]_0 = (x - r) \bmod 2^{32}$，$[x]_1 = r$。由构造可知，$[x]_0 + [x]_1 \equiv x - r + r \equiv x \pmod{2^{32}}$。

算术秘密分享的信息论安全性可由香农熵严格论证。对于服务器S1而言，其持有的份额 $[x]_0 = (x - r) \bmod 2^{32}}$ 为随机数 $r$ 的仿射变换。由于 $r$ 在 $\mathbb{Z}_{2^{32}}$ 上均匀分布，$[x]_0$ 亦在 $\mathbb{Z}_{2^{32}}$ 上均匀分布，与秘密值 $x$ 无关。因此，S1从 $[x]_0$ 中获取关于 $x$ 的信息量为零，即 $H(x \mid [x]_0) = H(x)$，其中 $H(\cdot)$ 表示香农熵。同理，S2从 $[x]_1$ 中亦无法获取关于 $x$ 的任何信息。仅当两方将份额聚合时，方可重构原始秘密：$x = ([x]_0 + [x]_1) \bmod 2^{32}}$。

算术秘密分享支持本地加法运算：设 $[x]$ 与 $[y]$ 分别为秘密值 $x$ 与 $y$ 的分享，则 $[x] + [y]$（即逐分量相加）构成 $x + y$ 的有效分享。该性质意味着加法运算无需任何交互即可完成，是算术秘密分享的核心效率优势。

#### 2.2.2 Beaver三元组与安全乘法

与本地加法不同，安全乘法无法由各方独立完成，因为 $([x]_0 + [x]_1) \cdot ([y]_0 + [y]_1)$ 的展开式中包含交叉项 $[x]_0 \cdot [y]_1$ 与 $[x]_1 \cdot [y]_0$，这些交叉项涉及来自不同服务器的私有份额，必须通过交互协议计算。Beaver于1991年提出的乘法三元组技术，通过将复杂的交互计算转移至离线阶段预生成辅助随机数，将在线安全乘法的通信轮数降至仅1轮，大幅提升了MPC的实用性。

**定义（Beaver乘法三元组）。** 设 $[a]$、$[b]$、$[c]$ 为三个独立的均匀随机分享，且满足 $c \equiv a \cdot b \pmod{2^{32}}$。则称 $([a], [b], [c])$ 为一个Beaver乘法三元组。

Beaver三元组可在离线阶段由可信第三方（Dealer）或专用安全协议生成。由于离线阶段不依赖于实际输入数据，三元组的生成可与传感器数据采集并行执行，从而隐藏其计算开销。给定一个预生成的三元组 $([a], [b], [c])$，两方在线安全乘法协议如下：

**协议1：安全乘法协议**

*输入：* 服务器S1持有 $[x]_0$、$[y]_0$、$[a]_0$、$[b]_0$、$[c]_0$；服务器S2持有 $[x]_1$、$[y]_1$、$[a]_1$、$[b]_1$、$[c]_1$。

*输出：* 两方各自获得 $[z]_0$ 与 $[z]_1$，满足 $z \equiv x \cdot y \pmod{2^{32}}$。

*协议步骤：*

1. 本地计算差值：S1计算 $e_0 = [x]_0 - [a]_0$，$f_0 = [y]_0 - [b]_0$；S2计算 $e_1 = [x]_1 - [a]_1$，$f_1 = [y]_1 - [b]_1$。

2. 公开差值：S1与S2交换 $e_0, f_0$ 与 $e_1, f_1$，双方本地重构 $e = (e_0 + e_1) \bmod 2^{32}$，$f = (f_0 + f_1) \bmod 2^{32}$。

3. 本地计算结果份额：S1计算 $[z]_0 = [c]_0 + e \cdot [b]_0 + f \cdot [a]_0 + e \cdot f$；S2计算 $[z]_1 = [c]_1 + e \cdot [b]_1 + f \cdot [a]_1$。

*正确性证明：*

$$\begin{aligned}
[z]_0 + [z]_1 &= [c]_0 + [c]_1 + e \cdot ([b]_0 + [b]_1) + f \cdot ([a]_0 + [a]_1) + e \cdot f \\
&= c + e \cdot b + f \cdot a + e \cdot f \\
&= a \cdot b + (x - a) \cdot b + (y - b) \cdot a + (x - a) \cdot (y - b) \\
&= x \cdot y \pmod{2^{32}}
\end{aligned}$$

上述协议仅包含一轮通信（步骤2中交换 $e, f$），通信量为两个32位整数的传输，具有极高的在线效率。

#### 2.2.3 安全比较协议

安全比较协议是MPC中实现关系运算（如 $<, >, =$）的核心组件。在算术秘密分享框架下，直接比较两个分享值的大小关系并非易事，因为份额在环 $\mathbb{Z}_{2^{32}}$ 上的加法结构无法直接反映底层整数的大小序关系。本文采用基于掩码（masking）的安全比较协议，其基本思想为：两方共同生成一个公开随机掩码 $r$，计算公开值 $d = x - r$，然后利用 $d$ 与 $r$ 的比特分解信息推断 $x$ 的符号位。

具体而言，设待比较的秘密值为 $x$，比较阈值为公开常数 $T$（在本文中 $T$ 为经过定点数缩放后的冲击阈值或姿态判定系数）。协议目标为在不泄露 $x$ 的前提下计算比特 $b = [x < T]$。协议的核心步骤如下：首先，两方通过安全协议生成 $x$ 的随机掩码分享，并公开 $d = x - r$；然后，将 $r$ 分解为二进制比特 $r_{31}r_{30}\cdots r_0$，将 $d$ 分解为 $d_{31}d_{30}\cdots d_0$；通过逐比特的安全运算，从最高有效位开始比较 $r$ 与 $d$ 的大小关系，最终提取出 $x - T$ 的符号位。由于 $d$ 被随机掩码 $r$ 掩盖，公开 $d$ 不会泄露 $x$ 的任何信息。

安全比较协议的通信复杂度略高于安全乘法协议，通常需要 $O(\lambda)$ 轮通信（其中 $\lambda$ 为安全参数，本文取32）。然而，在本文的两阶段跌倒检测算法中，每次检测仅需执行两次安全比较运算（一次冲击判定、一次姿态判定），通信开销在可接受范围内。

#### 2.2.4 半诚实安全模型

安全模型定义了协议设计中假设的对手（adversary）行为能力与腐败方式。本文采用**半诚实（semi-honest）安全模型**，也称为"诚实但好奇"（honest-but-curious）模型。在该模型下，被腐化的参与方会严格遵循协议规定的步骤执行计算，但会试图从协议交互的视图（view）中推断其他参与方的私有输入信息。半诚实模型适用于以下场景：参与方有动机保护自身数据隐私，但无动机破坏协议正确性；或者参与方的行为可被外部审计机制约束，恶意偏离协议将面临法律或经济惩罚。

半诚实模型的形式化定义通常通过**模拟范式**（simulation paradigm）表述：对于任何真实协议执行中被腐化方的视图，存在一个概率多项式时间模拟器，仅根据被腐化方的输入与输出，即可生成一个与真实视图计算不可区分的模拟视图。若该条件成立，则称协议在半诚实模型下是安全的。本文的两方架构在半诚实模型下的安全性基于以下事实：每一方仅持有均匀随机的秘密份额，单方份额与均匀随机数在信息论意义上完全不可区分；协议交互消息均为经过掩码处理的随机化值，不包含关于原始输入的可区分信息。

与恶意（malicious）安全模型相比，半诚实模型的协议设计更为简洁高效。恶意模型要求协议即使在参与方任意偏离协议的情况下仍能保证安全性，通常需要引入零知识证明、消息认证码或电路级一致性验证等额外机制，导致通信量与计算量大幅增加。考虑到跌倒检测系统的实际部署场景——云端服务器通常由可信的云服务提供商运营，恶意破坏协议的动机较弱——半诚实模型在安全性与效率之间取得了合理的平衡。

### 2.3 本章小结

本章系统阐述了本文研究所依赖的技术基础。在跌倒检测方面，介绍了基于合加速度幅值与倾斜角的两阶段阈值法原理，以及SisFall公开数据集的基本特征，并论证了阈值法在MPC隐私保护场景中的适配优势。在安全多方计算方面，详细论述了算术秘密分享的数学定义与信息论安全性、Beaver三元组的构造方法与1轮通信的安全乘法协议、基于掩码的安全比较协议的工作机制，以及半诚实安全模型的形式化定义与适用场景。上述理论构成为后续章节中系统架构设计、算法MPC改造与安全性分析奠定了坚实的基础。

---

## 第3章 隐私保护跌倒检测系统设计

### 3.1 系统架构设计

#### 3.1.1 端-云协同两方架构

本文设计了一种端-云协同的两方安全计算架构，由三个逻辑实体组成：客户端（可穿戴设备）、云端服务器S1与云端服务器S2。如图3-1所示，客户端负责原始传感器数据的采集、预处理与秘密分享生成；云端服务器S1与S2各自持有一份秘密份额，通过执行安全计算协议联合完成跌倒检测推理；检测完成后，两方将结果份额返回客户端，由客户端本地重构得到最终的布尔型跌倒判定。在整个流程中，任意单台服务器均无法从接收到的份额中还原出原始加速度数据，从而实现了原始传感器数据的全程隐私保护。

与现有研究采用的三方服务器架构相比，本文的两方架构具有以下优势：第一，**通信轮数更少**。三方架构要求三台服务器在每一轮运算中均进行广播或轮转通信，而两方架构仅需两台服务器之间的点对点交互，通信拓扑更为简洁。第二，**架构复杂度更低**。两方架构减少了需要部署、维护与监控的服务器节点数量，降低了系统运维成本与故障排查难度。第三，**实现更简单**。两方协议的状态同步与错误处理逻辑比多方协议更为直观，便于工程化实现与调试优化。

系统的数据流与控制流可概括为以下时序步骤：

- **Step 1（秘密分享）**：客户端读取IMU三轴加速度数据，执行定点数编码后，生成两个随机份额 $[share]_0$ 与 $[share]_1$，分别发送至服务器S1与S2。
- **Step 2（通道传输）**：秘密份额通过基于TLS加密的P2P安全通道传输至云端服务器，防止传输过程中被窃听或篡改。
- **Step 3（协同计算）**：S1与S2在本地Beaver三元组辅助下，执行安全乘法与安全比较协议，联合完成两阶段跌倒检测运算。
- **Step 4（结果重构）**：两方将检测结果份额返回客户端，客户端通过本地加法重构最终判定比特。

#### 3.1.2 与三方架构的对比分析

Mainali与Shepherd于2019年提出的MPC跌倒检测方案采用了典型的三方服务器架构，即由三台互不信任的服务器共同参与计算，客户端仅负责数据输入与结果接收。该架构在安全性方面具有一定的优势——即使单台服务器被攻破，另外两台服务器仍可保证数据不泄露。然而，三方架构在效率与实用性方面存在明显不足：

在通信复杂度方面，三方架构中的每一轮运算通常需要三台服务器之间进行两轮广播通信（每轮包含3条消息），而两方架构仅需一轮双向通信（每轮包含2条消息）。以安全乘法协议为例，三方架构的每轮乘法通常需要6至9条消息的交换，而两方Beaver乘法仅需2条消息。在跌倒检测这种需要频繁执行安全乘法与安全比较的应用中，通信轮数的差异将累积为显著的延迟差距。

在系统可用性方面，三方架构要求三台服务器同时在线且网络可达。若任意一台服务器发生故障或网络中断，整个系统将无法正常执行检测任务。两方架构仅需保证两台服务器的可用性，故障概率更低，系统可用性更高。

在部署成本方面，三方架构需要租用或维护三台独立的云服务器实例，而两方架构仅需两台。对于面向个人用户或中小型养老机构的健康监测服务，部署成本的差异将直接影响产品的市场可行性。

综上所述，本文选择两方架构作为系统设计基础，是在安全性、效率、可用性与成本之间权衡后的合理决策。在半诚实安全模型的假设下，两方架构足以满足跌倒检测场景中对数据隐私保护的需求。

### 3.2 客户端设计

#### 3.2.1 数据采集与预处理

客户端部署于可穿戴设备（如智能腰带、跌倒检测 pendant）中，核心硬件为一枚三轴MEMS加速度计。本文设定数据采集频率为100 Hz，该频率足以捕捉跌倒冲击阶段的瞬态加速度峰值（典型持续时间为100 ms至300 ms），同时避免了过高采样率带来的功耗与通信负担。每次检测窗口取1秒时长的加速度序列（100个采样点），计算该窗口内的SVM最大值作为冲击检测的输入特征，取冲击发生后0.5秒窗口内的平均加速度作为姿态检测的输入特征。

原始加速度数据以$g$为单位（$1g = 9.8 \text{ m/s}^2$），包含正负浮点数值。为了消除MPC密文域中的浮点运算开销，并避免不同硬件平台间浮点精度差异导致的计算不一致性，客户端在本地执行定点数编码转换。

#### 3.2.2 定点数编码

定点数编码是连接明文浮点算法与MPC整数运算的关键桥梁。本文采用缩放因子 $scale = 10000$ 的定点数表示方案：对于浮点值 $v$，其定点数表示为 $\hat{v} = \lfloor v \cdot scale \rceil$，其中 $\lfloor \cdot \rceil$ 表示四舍五入取整。定点数在环 $\mathbb{Z}_{2^{32}}$ 中进行运算，运算结果通过除以 $scale$（或乘以 $scale^{-1}$）还原为浮点近似值。

定点数编码的安全性在于：编码后的整数值在秘密分享框架下被拆分为随机份额，服务器无法从份额中判断原始值的大小、符号或量纲。因此，定点数编码不仅是一种性能优化手段，同时也是数据隐私保护机制的自然组成部分。

需要注意的是，定点数运算存在固有的精度损失与溢出风险。当两个缩放后的定点数相乘时，结果隐含了 $scale^2$ 的缩放因子，必须通过除以 $scale$ 进行 rescaling。本文在协议设计中严格控制中间结果的数值范围，确保在32位无符号整数环中不发生溢出——对于本文采用的 $scale = 10000$ 与加速度量程 $\pm 8g$，所有中间结果均可安全表示在 $\mathbb{Z}_{2^{32}}$ 中。

#### 3.2.3 秘密分享模块

秘密分享模块是客户端的核心密码学组件，负责将定点数编码后的传感器数据拆分为两个信息论安全的随机份额。设编码后的整数为 $x \in \mathbb{Z}_{2^{32}}$，模块执行以下操作：

1. 随机采样掩码 $r \xleftarrow{\$} \mathbb{Z}_{2^{32}}$；
2. 计算份额 $[x]_0 = (x - r) \bmod 2^{32}$，$[x]_1 = r$；
3. 将 $[x]_0$ 通过网络发送至服务器S1，将 $[x]_1$ 发送至服务器S2。

由2.2.1节的分析可知，$[x]_0$ 与 $[x]_1$ 各自在 $\mathbb{Z}_{2^{32}}$ 上均匀分布，任意单方无法从中获取关于 $x$ 的任何信息。客户端仅需保存用于最终结果重构的份额副本，或可选择不保存以进一步降低本地泄露风险。

#### 3.2.4 结果重构

当云端服务器完成安全计算后，两方将检测结果的秘密份额 $[result]_0$ 与 $[result]_1$ 返回客户端。客户端通过本地加法即可重构最终结果：

$$result = ([result]_0 + [result]_1) \bmod 2^{32}$$

在本文的两阶段跌倒检测算法中，$result$ 为 $\{0, 1\}$ 域中的布尔值（0表示未跌倒，1表示跌倒）。客户端无需执行任何复杂的解密运算，结果重构的计算开销可忽略不计。

### 3.3 云端服务器设计

#### 3.3.1 对称式服务器架构

本文的两台云端服务器S1与S2采用对称式设计，两者在硬件配置、软件协议栈与功能职责上完全一致。这种对称性简化了系统部署与负载均衡策略——无需为不同服务器维护差异化的配置或代码版本。在半诚实安全模型的假设下，两台服务器均严格遵循协议执行计算，但互不共享各自的私有视图。

每台服务器在本地维护以下核心数据结构：

- **输入份额缓冲区**：接收并暂存来自客户端的秘密份额；
- **Beaver三元组池**：离线预生成的乘法三元组队列，供在线安全乘法协议消耗；
- **安全计算引擎**：执行安全乘法、安全比较与本地加法运算的核心模块；
- **通信模块**：基于TCP长连接的网络通信接口，支持延迟模拟功能。

#### 3.3.2 离线阶段：Beaver三元组预生成

Beaver三元组的生成是安全乘法协议的离线预处理阶段。本文引入一个可信第三方角色——**Dealer**，负责在系统初始化阶段批量生成Beaver三元组并安全分发给两台服务器。Dealer的具体操作为：对每一个三元组，随机采样 $a, b \xleftarrow{\$} \mathbb{Z}_{2^{32}}$，计算 $c = a \cdot b \bmod 2^{32}$，然后生成 $[a], [b], [c]$ 的随机份额并分别发送至S1与S2。

在实际部署中，Dealer可由以下实体承担：专用的安全硬件模块（如硬件安全密钥管理器）、基于可信执行环境的隔离进程，或通过安全多方计算协议本身在S1与S2之间联合生成（消除对单一可信第三方的依赖）。本文的实验系统中采用本地批量生成的方式模拟Dealer功能，生成的三元组预先存储于服务器的三元组池中，在线阶段按需取用。

离线阶段的计算开销不直接影响在线检测的实时性，因为三元组的生成可在系统空闲时段或客户端尚未接入时批量完成。实验数据表明，Beaver三元组预生成仅占端到端延迟的约1%，对系统整体性能影响甚微。

#### 3.3.3 在线阶段：安全计算引擎

在线阶段，两台服务器基于接收到的输入份额与预生成的Beaver三元组，执行两阶段跌倒检测的安全计算。安全计算引擎实现了三类核心运算：

1. **本地加法**：对两个同类型的秘密份额执行模 $2^{32}$ 加法，无需通信；
2. **安全乘法**：调用协议1，消耗一个Beaver三元组，执行1轮通信；
3. **安全比较**：调用掩码比较协议，执行多轮通信（通常为 $O(\log 2^{32})$ 级别）。

引擎采用流水线式执行模型：当乘法运算等待通信响应时，引擎可调度其他独立的本地运算（如加法、中间结果缓存）填充空闲周期，从而最大化计算资源利用率。

#### 3.3.4 通信网络与延迟模拟

本文的实验系统采用基于Python socket的TCP长连接实现服务器间通信。TCP协议提供的可靠传输、有序交付与流量控制机制，确保了份额数据在网络传输过程中的完整性与正确性。为了评估系统在不同网络环境下的性能表现，实验系统中集成了网络延迟模拟模块：通过在发送与接收操作之间插入可控的时延（0 ms、10 ms、50 ms），模拟局域网、城域网与广域网场景下的通信延迟。该模块对于验证系统在实际部署环境中的鲁棒性具有重要意义。

### 3.4 隐私保护机制分析

#### 3.4.1 全生命周期隐私保护

本文的系统设计实现了跌倒检测全流程中的隐私保护，覆盖数据采集、传输、计算与输出四个阶段：

- **采集阶段**：原始加速度数据在客户端本地完成定点数编码后，立即通过秘密分享模块拆分为随机份额。原始整数数据在内存中仅短暂存在，随后被份额替代，避免了本地存储泄露风险。
- **传输阶段**：秘密份额通过TLS加密的P2P安全通道传输至云端服务器。即使网络链路被窃听，攻击者获取的也仅是信息论安全的随机份额，无法还原原始数据。
- **计算阶段**：云端服务器在份额域上执行安全计算协议。所有中间结果均以秘密份额的形式存在，服务器无法从任何中间量中推断原始输入。半诚实模型下，服务器的视图仅包含均匀随机的份额与掩码值。
- **输出阶段**：系统最终仅向客户端输出布尔型的跌倒判定结果（0或1），不包含任何原始传感器数据或中间特征值。即使客户端被攻破，攻击者也无法从单一比特的检测结果中反推原始加速度序列。

#### 3.4.2 安全性证明思路

本文方案在半诚实两方模型下的安全性可通过模拟范式严格证明。以服务器S1的视图为例，其协议执行视图包含以下内容：接收到的输入份额 $[x]_0$（均匀随机数）、离线阶段接收到的Beaver三元组份额（均匀随机数）、协议交互过程中公开的差值 $e, f$（被随机掩码掩盖的均匀随机数）。由于上述所有元素在信息论意义上均与均匀随机数不可区分，存在模拟器 $\mathcal{S}$ 可在不获知S2输入与客户端原始数据的前提下，生成与真实视图统计不可区分的模拟视图。因此，S1从协议执行中无法获取关于其他方私有输入的任何信息。同理可证S2的安全性。客户端的视图包含最终结果比特，该比特为协议的正确输出，不泄露额外信息。

### 3.5 本章小结

本章提出了端-云协同的隐私保护跌倒检测系统架构。首先，设计了两方安全计算架构，阐明了其与三方架构在通信轮数、系统复杂度与部署成本方面的优势。其次，详细设计了客户端模块，包括100 Hz数据采集、$scale = 10000$ 定点数编码、32位整数环上的算术秘密分享生成与结果重构机制。再次，设计了云端服务器的对称式架构，涵盖离线阶段的Beaver三元组预生成与在线阶段的安全计算引擎。最后，对系统全生命周期的隐私保护机制进行了分析，并给出了半诚实模型下安全性证明的思路框架。下一章将深入阐述两阶段跌倒检测算法的明文设计及其MPC安全改造过程。

---

## 第4章 两阶段跌倒检测算法及MPC安全改造

### 4.1 明文跌倒检测算法设计

#### 4.1.1 冲击检测阶段

冲击检测阶段的核心任务是判定在检测时间窗口内是否发生了剧烈的加速度冲击事件，这通常对应于人体躯干与地面的碰撞。本文采用合加速度幅值（SVM）作为冲击检测的判别特征。设三轴加速度分量分别为 $a_x, a_y, a_z$（单位：m/s²），则SVM定义为：

$$SVM = \sqrt{a_x^2 + a_y^2 + a_z^2} \tag{4-1}$$

冲击检测的判定逻辑为：若当前窗口内的最大SVM值超过预设的冲击阈值 $T_{impact}$，则判定冲击事件发生。即：

$$impact = \begin{cases} 1, & \text{if } \max(SVM) > T_{impact} \\ 0, & \text{otherwise} \end{cases} \tag{4-2}$$

#### 4.1.2 姿态检测阶段

姿态检测阶段用于区分跌倒冲击后的躺卧姿态与其他非跌倒低姿态活动（如弯腰、蹲下）。本文采用躯干倾斜角 $\theta$ 作为姿态判别的特征量。倾斜角定义为人体躯干纵轴与重力方向的夹角，可通过加速度矢量在竖直方向的分量 $a_z$ 与SVM的比值计算：

$$\theta = \arccos\left(\frac{a_z}{SVM}\right) \tag{4-3}$$

姿态检测的判定逻辑为：若冲击发生后0.5秒窗口内的平均倾斜角超过预设的姿态角度阈值 $\theta_{th}$，则判定人体处于躺卧姿态。即：

$$posture = \begin{cases} 1, & \text{if } \bar{\theta} > \theta_{th} \\ 0, & \text{otherwise} \end{cases} \tag{4-4}$$

#### 4.1.3 最终判定与阈值参数实验调优

最终跌倒判定为冲击检测与姿态检测的逻辑与：

$$fall = impact \land posture \tag{4-5}$$

仅当冲击事件与躺卧姿态同时被检测到时，系统才输出跌倒警报。

**阈值参数的实验调优过程**是本文算法设计的重要组成部分，体现了数据驱动的科学严谨性。在冲击阈值的选择方面，已有文献（如Kangas等人[1]）通常建议采用3$g$（约29.4 m/s²）作为跌倒冲击的判别阈值。然而，本文在SisFall数据集上的验证实验表明，3$g$阈值对于部分跌倒模式（尤其是因晕厥导致的缓慢瘫软跌倒）存在较高的漏检率。通过逐步降低阈值并在验证集上评估检测性能，本文最终确定 $T_{impact} = 2g = 19.6 \text{ m/s}^2$ 为最优冲击阈值。该阈值在保证对日常活动低误报率的同时，显著降低了跌倒事件的漏检率。

在姿态角度阈值的选择方面，初始实验采用文献中常见的 $45°$ 作为判别标准。然而，实验发现该阈值在"严格模式"（即要求两阶段均严格判定为阳性）下导致部分跌倒样本被误判为正常活动。分析原因可知，部分跌倒事件中人体并未完全水平躺卧（例如臀部着地、上身部分抬起），实际倾斜角可能仅在50°至60°之间。通过将姿态角度阈值逐步放宽至 $60°$，系统在严格模式下的检测成功率显著提升，同时未观察到日常活动误报率的明显上升。最终，本文确定姿态角度阈值为 $\theta_{th} = 60°$。

### 4.2 MPC等价变换

将明文两阶段阈值算法改造为MPC安全版本的核心挑战在于：明文算法中的开方运算（式4-1）、除法运算（式4-3）与反三角函数运算（式4-3）在MPC密文域中的实现代价极高。本节通过严格的数学等价变换，将上述运算完全消除，使MPC安全版本仅需安全乘法与安全比较两种基础运算。

#### 4.2.1 冲击判定变换：消除开方运算

明文冲击判定条件为 $\max(SVM) > T_{impact}$，其中 $SVM = \sqrt{a_x^2 + a_y^2 + a_z^2}$。由于 $SVM$ 与 $T_{impact}$ 均为正数（加速度幅值与冲击阈值均为非负量），平方函数 $f(x) = x^2$ 在正数域上严格单调递增。因此，对不等式两边同时平方不改变不等号方向：

$$\sqrt{a_x^2 + a_y^2 + a_z^2} > T_{impact} \iff a_x^2 + a_y^2 + a_z^2 > T_{impact}^2 \tag{4-6}$$

令 $SVM^2 = a_x^2 + a_y^2 + a_z^2$，$T_{impact}^2$ 为可预先计算的公开常数，则冲击判定条件转化为：

$$impact = \begin{cases} 1, & \text{if } SVM^2 > T_{impact}^2 \\ 0, & \text{otherwise} \end{cases} \tag{4-7}$$

该变换将开方运算完全消除。在MPC实现中，$SVM^2$ 的计算需要两次安全乘法（$a_x^2 = a_x \cdot a_x$，$a_y^2 = a_y \cdot a_y$，$a_z^2 = a_z \cdot a_z$）与两次本地加法（$SVM^2 = a_x^2 + a_y^2 + a_z^2$），随后执行一次安全比较（$SVM^2 > T_{impact}^2$）。

#### 4.2.2 姿态判定变换：消除除法与反三角函数运算

姿态判定是MPC改造中最核心的环节，涉及消除除法运算与反三角函数运算。明文姿态判定条件为 $\bar{\theta} > \theta_{th}$，其中 $\theta = \arccos(a_z / SVM)$。本节给出完整的数学等价变换推导。

**步骤1：利用arccos单调性转化角度比较。**

反余弦函数 $\arccos(x)$ 在其定义域 $[-1, 1]$ 上严格单调递减。因此，对于任意 $x_1, x_2 \in [-1, 1]$，有：

$$x_1 < x_2 \iff \arccos(x_1) > \arccos(x_2) \tag{4-8}$$

将 $x_1 = a_z / SVM$、$x_2 = \cos(\theta_{th})$ 代入式（4-8），得：

$$\arccos\left(\frac{a_z}{SVM}\right) > \theta_{th} \iff \frac{a_z}{SVM} < \cos(\theta_{th}) \tag{4-9}$$

**步骤2：消除除法运算。**

由于 $SVM = \sqrt{a_x^2 + a_y^2 + a_z^2} > 0$（加速度幅值恒为正，仅当三轴同时为零时 $SVM = 0$，而该情况在实际传感器数据中不会出现），不等式两边同乘正数 $SVM$ 不改变不等号方向：

$$\frac{a_z}{SVM} < \cos(\theta_{th}) \iff a_z < SVM \cdot \cos(\theta_{th}) \tag{4-10}$$

**步骤3：消除符号依赖。**

为确保后续平方操作的安全性，需对不等式两边取绝对值。然而，在本文的应用场景中，跌倒发生后人体通常处于水平或接近水平姿态，$a_z$ 为重力加速度在竖直方向的分量。更简洁的处理方式是直接对两边平方——由于不等式两边在跌倒判定的感兴趣区域内均为非负（$a_z > 0$ 且 $SVM \cdot \cos(\theta_{th}) > 0$），平方操作保持不等号方向：

$$a_z < SVM \cdot \cos(\theta_{th}) \iff a_z^2 < SVM^2 \cdot \cos^2(\theta_{th}) \tag{4-11}$$

**步骤4：消除乘法系数。**

令 $k = 1 / \cos^2(\theta_{th})$，由于 $\cos^2(\theta_{th}) > 0$，不等式两边同乘 $k$ 不改变不等号方向：

$$a_z^2 < SVM^2 \cdot \cos^2(\theta_{th}) \iff k \cdot a_z^2 < SVM^2 \tag{4-12}$$

代入本文确定的姿态角度阈值 $\theta_{th} = 60°$，有 $\cos(60°) = 0.5$，$\cos^2(60°) = 0.25$，$k = 1 / 0.25 = 4$。因此，最终的MPC姿态判定条件为：

$$posture = \begin{cases} 1, & \text{if } 4 \cdot a_z^2 < SVM^2 \\ 0, & \text{otherwise} \end{cases} \tag{4-13}$$

该变换将姿态判定完全转化为仅涉及安全乘法（$a_z^2 = a_z \cdot a_z$）与安全比较（$4 \cdot a_z^2 < SVM^2$）的运算。其中乘以常数4的操作可在本地完成（各方将自有份额乘以4），无需任何通信。

#### 4.2.3 逻辑与的MPC实现

最终跌倒判定为冲击判定比特与姿态判定比特的逻辑与：$fall = impact \land posture$。在MPC框架下，逻辑与运算可通过算术乘法实现。具体而言，当 $impact, posture \in \{0, 1\}$ 时，在整数环 $\mathbb{Z}_{2^{32}}$ 中有：

$$impact \land posture = impact \cdot posture \pmod{2^{32}} \tag{4-14}$$

该等式可通过穷举验证：当 $(impact, posture) = (0, 0)$ 时，$0 \cdot 0 = 0$；当 $(0, 1)$ 时，$0 \cdot 1 = 0$；当 $(1, 0)$ 时，$1 \cdot 0 = 0$；当 $(1, 1)$ 时，$1 \cdot 1 = 1$。四种情况均与逻辑与的真值表一致。

因此，最终的跌倒判定仅需一次安全乘法运算：两方在 $impact$ 与 $posture$ 的秘密份额上执行协议1，得到 $[fall] = [impact] \cdot [posture]$。

### 4.3 MPC安全协议流程

基于上述等价变换，本节以协议伪代码的形式给出完整的MPC两阶段跌倒检测流程。

**协议2：安全比较协议**

*输入：* 两方持有秘密值 $[x]$，公开阈值 $T$。

*输出：* 两方获得 $[b]$，其中 $b = 1$ 当且仅当 $x < T$，否则 $b = 0$。

*协议步骤：*

1. 两方协同生成随机掩码 $[r]$，并公开 $d = x - r$；
2. 对 $r$ 与 $d$ 分别进行二进制比特分解；
3. 从最高有效位至最低有效位逐位比较，通过安全运算提取符号位；
4. 输出符号位的秘密分享 $[b]$。

**协议3：两阶段隐私保护跌倒检测协议**

*输入：* 客户端持有原始三轴加速度 $(a_x, a_y, a_z)$；S1持有 $[a_x]_0, [a_y]_0, [a_z]_0$；S2持有 $[a_x]_1, [a_y]_1, [a_z]_1$。公开参数：$T_{impact}^2$（缩放后的冲击阈值平方），$k = 4$（姿态判定系数）。

*输出：* 客户端获得比特 $fall \in \{0, 1\}$。

*协议步骤：*

**阶段一：客户端本地预处理**

1. 客户端对 $(a_x, a_y, a_z)$ 执行定点数编码：$\hat{a}_x = a_x \cdot scale$，$\hat{a}_y = a_y \cdot scale$，$\hat{a}_z = a_z \cdot scale$；
2. 生成秘密份额 $[\hat{a}_x], [\hat{a}_y], [\hat{a}_z]$，分别发送至S1与S2。

**阶段二：云端安全计算（S1与S2协同执行）**

3. **冲击检测**：
   - 计算 $[\hat{a}_x^2] = [\hat{a}_x] \cdot [\hat{a}_x]$（协议1，安全乘法）；
   - 计算 $[\hat{a}_y^2] = [\hat{a}_y] \cdot [\hat{a}_y]$（协议1，安全乘法）；
   - 计算 $[\hat{a}_z^2] = [\hat{a}_z] \cdot [\hat{a}_z]$（协议1，安全乘法）；
   - 本地加法计算 $[SVM^2] = [\hat{a}_x^2] + [\hat{a}_y^2] + [\hat{a}_z^2]$；
   - 执行 $[impact] = [SVM^2 > T_{impact}^2]$（协议2，安全比较）。

4. **姿态检测**：
   - 本地计算 $[k \cdot \hat{a}_z^2]$（各方将份额乘以常数4）；
   - 执行 $[posture] = [k \cdot \hat{a}_z^2 < SVM^2]$（协议2，安全比较）。

5. **最终判定**：
   - 计算 $[fall] = [impact] \cdot [posture]$（协议1，安全乘法）。

**阶段三：结果重构**

6. S1与S2将 $[fall]_0$ 与 $[fall]_1$ 返回客户端；
7. 客户端计算 $fall = ([fall]_0 + [fall]_1) \bmod 2^{32}$。

*通信复杂度分析：* 每次检测需要执行4次安全乘法（3次用于平方计算，1次用于逻辑与）与2次安全比较。每次安全乘法消耗1轮通信，安全比较消耗 $O(\log 2^{32})$ 轮通信。总计通信轮数为 $4 + 2 \cdot O(32) = O(68)$ 轮。

### 4.4 正确性与安全性分析

#### 4.4.1 计算正确性

本文MPC安全版本与明文版本的计算正确性由4.2节中的数学等价变换严格保证。

**定理4.1（冲击判定等价性）。** 对于任意实数 $a_x, a_y, a_z$ 与正数 $T_{impact}$，有：

$$\sqrt{a_x^2 + a_y^2 + a_z^2} > T_{impact} \iff a_x^2 + a_y^2 + a_z^2 > T_{impact}^2$$

*证明：* 由于 $SVM = \sqrt{a_x^2 + a_y^2 + a_z^2} \geq 0$ 且 $T_{impact} > 0$，函数 $f(x) = x^2$ 在 $[0, +\infty)$ 上严格单调递增。因此，$SVM > T_{impact} \iff SVM^2 > T_{impact}^2$。代入 $SVM^2 = a_x^2 + a_y^2 + a_z^2$，即得证。$\square$

**定理4.2（姿态判定等价性）。** 对于任意实数 $a_z, SVM$ 满足 $0 < a_z \leq SVM$ 与角度 $\theta_{th} \in (0°, 90°)$，有：

$$\arccos\left(\frac{a_z}{SVM}\right) > \theta_{th} \iff \frac{1}{\cos^2(\theta_{th})} \cdot a_z^2 < SVM^2$$

*证明：* 由 $\arccos(x)$ 在 $[-1, 1]$ 上严格单调递减，得 $\arccos(a_z / SVM) > \theta_{th} \iff a_z / SVM < \cos(\theta_{th})$。由于 $SVM > 0$，两边同乘 $SVM$ 得 $a_z < SVM \cdot \cos(\theta_{th})$。由于 $\theta_{th} \in (0°, 90°)$，有 $\cos(\theta_{th}) > 0$，故两边同非负，平方得 $a_z^2 < SVM^2 \cdot \cos^2(\theta_{th})$。由于 $\cos^2(\theta_{th}) > 0$，两边同乘 $1 / \cos^2(\theta_{th})$，即得 $a_z^2 / \cos^2(\theta_{th}) < SVM^2$。$\square$

由定理4.1与定理4.2可知，MPC安全版本在数学上与明文版本完全等价。两者的输出差异仅可能来源于定点数编码引入的量化误差与32位整数环上的模运算截断，该差异在实验评估中被量化为约2%的精度损失。

#### 4.4.2 安全性分析

本文方案在半诚实两方模型下的安全性基于以下两个核心事实：

**事实1（份额的不可区分性）。** 对于任意秘密值 $x \in \mathbb{Z}_{2^{32}}$，其单方份额 $[x]_i$（$i \in \{0, 1\}$）在 $\mathbb{Z}_{2^{32}}$ 上均匀分布。即，对于任意 $v \in \mathbb{Z}_{2^{32}}$，有 $\Pr[[x]_i = v] = 1 / 2^{32}}$，与 $x$ 无关。

*证明：* 由份额构造 $[x]_0 = (x - r) \bmod 2^{32}$，$[x]_1 = r$，其中 $r \xleftarrow{\$} \mathbb{Z}_{2^{32}}$ 为均匀随机数。对于固定的 $x$，$x - r$ 随 $r$ 的均匀随机性而在 $\mathbb{Z}_{2^{32}}$ 上均匀分布。$\square$

**事实2（交互消息的不可区分性）。** 在安全乘法协议中，公开的差值 $e = x - a$ 与 $f = y - b$ 被随机掩码 $a, b$ 掩盖。由于 $a, b$ 为均匀随机数，$e$ 与 $f$ 亦在 $\mathbb{Z}_{2^{32}}$ 上均匀分布，与 $x, y$ 无关。

*证明：* $e = x - a$，$a \xleftarrow{\$} \mathbb{Z}_{2^{32}}$。对于固定的 $x$，$x - a$ 随 $a$ 的均匀性而均匀分布。同理可证 $f$。$\square$

基于上述事实，可构造模拟器证明协议安全性。以服务器S1为例，其视图包含：输入份额（事实1保证其不可区分于随机数）、Beaver三元组份额（离线生成，均匀随机）、交互差值（事实2保证其不可区分于随机数）。因此，存在模拟器可在仅获知S1输入份额的前提下，生成与真实视图统计不可区分的模拟视图，满足半诚实模型下的安全性定义。

### 4.5 本章小结

本章是本文的核心技术章节。首先，给出了明文两阶段阈值跌倒检测算法的完整设计，包括冲击检测阶段的SVM幅值比较、姿态检测阶段的倾斜角比较，以及基于实验数据驱动的阈值参数调优过程（$T_{impact} = 2g = 19.6 \text{ m/s}^2$，$\theta_{th} = 60°$）。其次，通过严格的数学推导，给出了明文算法到MPC安全版本的等价变换：冲击判定中的开方运算通过平方单调性消除；姿态判定中的除法与反三角函数运算通过余弦单调性、代数变形与系数归一化消除；最终逻辑与通过{0,1}比特上的安全乘法实现。变换后的MPC协议仅依赖安全乘法与安全比较两种基础运算。再次，以协议伪代码的形式描述了完整的隐私保护跌倒检测流程，并分析了通信复杂度。最后，从计算正确性与协议安全性两个维度给出了严格的理论分析，证明了MPC版本与明文版本的数学等价性，以及半诚实模型下的信息论安全性。下一章将在上述协议设计的基础上，开展系统实现与全面的性能评估实验。

---

## 第5章 系统实现与性能评估

### 5.1 实验环境与数据集

#### 5.1.1 软件与硬件环境

本文的实验系统基于Python 3.9实现，主要依赖NumPy库进行数值计算与矩阵运算，依赖Python标准库socket实现TCP网络通信。实验在单机多进程环境下进行，通过三个独立进程分别模拟客户端、云端服务器S1与云端服务器S2的角色。尽管实际部署中三台物理设备将分布于不同地理位置，但单机多进程仿真在协议逻辑、通信时序与计算复杂度方面与真实分布式环境完全等价，差异仅在于网络层延迟的可控性——本文通过网络延迟模拟模块精确复现了不同网络条件下的通信延迟。

实验主机配置为Intel Core i5处理器、8 GB内存、Windows 10操作系统。需要指出的是，本文实验并非追求极致的性能优化，而是验证方案的可行性与评估各组件的相对开销。在实际部署中，采用C/C++语言实现并结合硬件加速（如AVX指令集、GPU并行计算），系统的延迟性能可进一步优化。

#### 5.1.2 数据集与实验设置

本文选用SisFall公开数据集作为实验数据源。数据预处理流程如下：首先，遍历数据集中所有以"F"（Fall，跌倒）与"D"（Daily activity，日常活动）开头的文本文件，读取时间戳与三轴加速度值；其次，执行数据清洗，剔除采样异常或数值跳变的记录；最后，将加速度单位从$g$转换为m/s²（乘以9.8），并按照本文设定的检测窗口长度（冲击检测取1秒、姿态检测取0.5秒）提取样本。

在数据集划分方面，本文从SisFall数据集中随机抽取50例跌倒样本与50例正常活动样本，构成总规模为100的测试集。该测试集规模虽然不大，但已足以验证MPC协议的正确性与评估精度损失程度，同时避免了大规模实验带来的冗长计算时间。所有实验结果均在相同的测试集与参数配置下重复运行多次取平均值，以保证结果的稳定性与可复现性。

### 5.2 正确性验证

#### 5.2.1 明文与MPC混淆矩阵对比

为验证MPC安全版本与明文版本的计算一致性，本文在相同的100样本测试集上分别运行了明文检测算法与MPC隐私保护检测算法，并记录了两者的混淆矩阵。如表5-1所示，明文检测算法正确识别了48例正常样本与48例跌倒样本，产生了2例假阳性（将正常活动误判为跌倒）与2例假阴性（将跌倒事件漏检）。MPC隐私保护检测算法正确识别了47例正常样本与47例跌倒样本，产生了3例假阳性与3例假阴性。

**表5-1 明文检测与MPC检测混淆矩阵对比**

| 算法 | 真实正常/预测正常 | 真实正常/预测跌倒 | 真实跌倒/预测正常 | 真实跌倒/预测跌倒 |
|:---:|:---:|:---:|:---:|:---:|
| 明文检测 | 48 | 2 | 2 | 48 |
| MPC检测 | 47 | 3 | 3 | 47 |

#### 5.2.2 输出一致性分析

对比两个混淆矩阵可知，明文版本与MPC版本在100个测试样本中有99个样本的判定结果一致，仅1个样本存在差异。该差异源于定点数编码引入的量化误差：在定点数表示下，浮点值经过缩放与四舍五入后损失了部分小数精度，导致处于判定边界附近的个别样本（其特征值非常接近阈值）出现了不同的判定结果。具体而言，该样本的 $SVM^2$ 或 $4 \cdot a_z^2$ 值在明文浮点计算中略低于阈值，而在定点数整数计算中略高于阈值（或反之），从而导致判定翻转。

需要强调的是，上述差异是定点数量化误差的固有现象，而非协议设计缺陷。在实际工程实现中，可通过增大定点数缩放因子（如将 $scale$ 从10000提升至100000）或采用更高精度的整数环（如64位整数）来进一步降低量化误差。然而，缩放因子的增大将同时增加乘法运算的溢出风险，需要在精度与安全性之间进行权衡。本文采用的 $scale = 10000$ 在32位整数环上实现了合理的精度-安全平衡。

### 5.3 精度对比分析

#### 5.3.1 四指标性能评估

为全面评估MPC隐私保护方案对检测性能的影响，本文在100样本测试集上计算了Accuracy、Precision、Recall与F1-Score四项指标，并与明文基线进行了对比。如表5-2所示，明文检测取得了96.0%的准确率、96.0%的精确率、96.0%的召回率与0.960的F1值。MPC隐私保护检测取得了94.0%的准确率、94.0%的精确率、94.0%的召回率与0.940的F1值。

**表5-2 明文检测与MPC检测性能指标对比**

| 方案 | Accuracy (%) | Precision (%) | Recall (%) | F1-Score |
|:---:|:---:|:---:|:---:|:---:|
| 明文检测 | 96.0 | 96.0 | 96.0 | 0.960 |
| MPC检测 | 94.0 | 94.0 | 94.0 | 0.940 |

#### 5.3.2 精度损失原因分析

MPC方案相较于明文基线的精度损失约为2个百分点，该损失完全来源于定点数编码引入的量化误差，而非MPC协议本身的计算错误（由第4章的等价性定理保证）。量化误差的产生机制可从以下三个层面进行分析：

第一，**缩放截断误差**。浮点值 $v$ 经定点数编码为 $\hat{v} = \lfloor v \cdot scale \rceil$，其中四舍五入操作引入了最大为 $0.5 / scale = 0.00005$ 的绝对误差。在比较判定中，当特征值与阈值非常接近时，该微小误差可能导致判定结果翻转。

第二，**乘法溢出风险**。两个32位定点数相乘的结果最大可达 $(2^{32} - 1)^2$，远超32位整数表示范围。尽管本文通过参数设计避免了实际溢出，但在极端输入条件下（如异常高的加速度冲击），仍存在溢出导致结果截断的风险。

第三，**模运算回绕**。算术秘密分享在环 $\mathbb{Z}_{2^{32}}$ 中进行，当加法或乘法结果超过 $2^{32} - 1$ 时，将发生模回绕（modular wrap-around）。虽然正常范围内的运算不会触发回绕，但模运算的固有性质要求在协议设计中严格控制数值范围。

综上所述，本文MPC方案的精度损失在可接受范围内，且可通过提高整数位宽或优化缩放策略进一步降低。

### 5.4 延迟性能分析

#### 5.4.1 不同输入规模的端到端延迟

本文测试了1样本、10样本与50样本三种输入规模下的端到端检测延迟。如表5-3所示，单样本检测延迟约为700 ms，10样本批量检测延迟约为920 ms，50样本批量检测延迟约为1780 ms。延迟随样本数量增长的趋势并非线性，原因在于：安全乘法协议中的通信开销（网络往返时延）在批量处理时可被部分摊销——当多个样本的乘法运算使用独立的三元组时，部分本地计算可并行调度，而通信往返的固定开销（TCP确认、协议栈处理等）在单位样本上的占比随批量增大而降低。

**表5-3 不同输入规模的端到端延迟**

| 输入规模 | 端到端延迟 (ms) |
|:---:|:---:|
| 1样本 | 700 |
| 10样本 | 920 |
| 50样本 | 1780 |

#### 5.4.2 延迟组件分解

为定位系统的性能瓶颈，本文对单样本检测的端到端延迟进行了组件级分解。如表5-4所示，安全乘法运算占据了总延迟的64%至67%，安全比较运算占据了29%至31%，客户端预处理（包括定点数编码与秘密分享生成）占据了约6%，离线预生成的Beaver三元组加载占据了约1%。

**表5-4 单样本延迟组件分解**

| 组件 | 延迟占比 (%) |
|:---:|:---:|
| 安全乘法 | 64–67 |
| 安全比较 | 29–31 |
| 客户端预处理 | ~6 |
| 离线预生成（三元组加载） | ~1 |

上述分解结果表明，**安全乘法是系统的绝对性能瓶颈**，其高延迟主要来源于以下因素：每次安全乘法需要消耗一个预生成的Beaver三元组，涉及 $e, f$ 差值的计算、网络传输与结果份额的本地重构；在单次跌倒检测中，冲击检测阶段需要执行3次安全乘法（三轴分量的平方计算），最终判定需要1次安全乘法（逻辑与），共计4次安全乘法调用。安全比较运算虽然单次耗时低于安全乘法，但每次检测需要执行2次安全比较（冲击判定与姿态判定），累积延迟占比亦不可忽视。

#### 5.4.3 设计目标分析

本文设定300 ms作为理想情况下的设计目标延迟阈值。当前单样本700 ms的延迟尚未达到该目标，但已在可接受范围内（对于非毫秒级强实时性要求的健康监测场景，秒级延迟仍可满足临床辅助需求）。未来优化方向包括：采用批量Beaver三元组生成策略，减少三元组调度开销；引入并行安全比较协议，利用SIMD指令同时处理多个比特位的比较运算；将部分计算迁移至客户端，减少服务器间交互次数。

### 5.5 综合方案对比

为全面评估本文方案在隐私保护领域的竞争力，本文将其与四种典型方案进行了多维度对比：明文上传（无保护）、联邦学习（中等保护）、全同态加密（高保护、高延迟）与本文MPC方案。如表5-5所示，对比维度涵盖隐私保护强度、端到端延迟、计算开销、通信开销与硬件依赖五个方面。

**表5-5 隐私保护方案综合对比**

| 维度 | 明文上传 | 联邦学习 | 全同态加密 | 本文MPC方案 |
|:---|:---:|:---:|:---:|:---:|
| 隐私保护强度 | 1 | 3 | 5 | 5 |
| 端到端延迟 | <1 ms | 10–50 ms | >5000 ms | ~700 ms |
| 计算开销 | 极低 | 低 | 极高 | 中等 |
| 通信开销 | 低 | 中等 | 高 | 中等 |
| 硬件依赖 | 无 | 无 | 无 | 无 |

在隐私保护强度方面，本文MPC方案与全同态加密均达到了最高等级（5分），实现了信息论级别的数据保护；联邦学习由于仅保护模型参数，原始推理数据仍暴露于本地，保护强度为中等（3分）；明文上传无保护（1分）。

在端到端延迟方面，明文上传具有最低的延迟（<1 ms），但牺牲了全部隐私；联邦学习的延迟主要取决于本地模型推理时间（10–50 ms）；全同态加密的延迟最高（>5000 ms），难以满足实时性要求；本文MPC方案以约700 ms的延迟实现了强隐私保护，在隐私-效率权衡中取得了较优的平衡。

在计算开销方面，本文MPC方案的计算开销显著低于全同态加密，但高于明文检测与联邦学习。这一开销是获得强隐私保护的必要代价，在医疗物联网场景下通常可被接受。

### 5.6 网络鲁棒性测试

#### 5.6.1 不同网络延迟下的性能变化

为验证系统在不同网络环境下的稳定性，本文在0 ms（本地回环）、10 ms（模拟局域网）与50 ms（模拟广域网）三种网络延迟条件下进行了延迟测试。实验结果表明，网络延迟的增加对系统端到端延迟具有近似线性的影响：在10 ms网络延迟下，单样本检测延迟约为720 ms；在50 ms网络延迟下，单样本检测延迟约为800 ms。由于安全乘法与安全比较协议均涉及服务器间的往返通信，每增加1 ms的网络RTT，总延迟约增加2至4 ms（取决于协议轮数）。

#### 5.6.2 系统稳定性结论

尽管网络延迟的增加会线性提升端到端延迟，但系统的功能正确性与判定精度不受网络延迟影响——MPC协议的正确性仅依赖于份额的数学结构，与通信时序无关。因此，本文方案在不同网络条件下均保持了94.0%的检测精度，表现出良好的鲁棒性。

### 5.7 本章小结

本章从正确性验证、精度对比、延迟分解、综合方案对比与网络鲁棒性五个维度，对本文提出的隐私保护跌倒检测系统进行了全面的性能评估。实验结果表明：MPC安全版本与明文版本在100样本测试集上具有99%的输出一致性，1个差异样本源于定点数量化误差；MPC方案的检测精度为94.0%，相较于明文基线96.0%产生约2%的精度损失，满足设计要求；单样本端到端延迟约为700 ms，其中安全乘法占64%至67%，安全比较占29%至31%；与联邦学习、全同态加密等方案相比，本文MPC方案在隐私保护强度与计算效率之间取得了较优的均衡；系统在不同网络延迟条件下保持了功能正确性与精度稳定性。上述实验结果为本文方案的实际部署可行性提供了充分的数据支撑。

---

## 第6章 总结与展望

### 6.1 工作总结

本文针对穿戴式跌倒检测系统中原始传感器数据的隐私泄露问题，设计并实现了一种基于高效安全多方计算的隐私保护跌倒检测系统。本文的主要研究成果可总结如下：

第一，**构建了面向跌倒检测的MPC基础协议库**。本文基于算术秘密分享与Beaver三元组技术，实现了两方半诚实模型下的安全乘法协议（1轮通信）与安全比较协议，为跌倒检测算法的MPC改造提供了底层密码学工具支撑。

第二，**完成了两阶段阈值算法的MPC安全改造**。通过严格的数学等价变换，本文将明文算法中的开方运算、除法运算与反三角函数运算完全消除，证明了冲击判定可转化为平方项的比较、姿态判定可转化为 $4 \cdot a_z^2 < SVM^2$ 的线性比较、最终逻辑与可通过{0,1}比特上的安全乘法实现。上述变换将协议复杂度降至仅依赖安全乘法与安全比较两种基础运算。

第三，**搭建了端-云协同的仿真验证系统**。本文设计了两方安全计算架构，采用多进程模拟客户端与两台云端服务器的交互，基于TCP网络通信实现了端到端的隐私保护检测流程，并支持网络延迟模拟功能。

第四，**实验验证了方案的可行性与性能指标**。在SisFall公开数据集的100样本测试集上，本文方案取得了94.0%的准确率、94.0%的精确率、94.0%的召回率与0.940的F1值，相较于明文基线仅产生约2%的精度损失；单样本端到端延迟约为700 ms，满足非强实时性医疗辅助检测的设计要求。

### 6.2 未来展望

尽管本文在MPC隐私保护跌倒检测方面取得了一定进展，但仍有若干方向值得在后续研究中深入探索：

**恶意安全模型扩展。** 本文采用半诚实安全模型，假设参与方不会偏离协议规范。在实际部署中，若云端服务器被高级持续威胁（Advanced Persistent Threat, APT）攻陷，攻击者可能通过发送错误的中间结果来破坏检测正确性。未来的研究可引入零知识证明（Zero-Knowledge Proof, ZKP）或消息认证码（Message Authentication Code, MAC）机制，将安全性提升至恶意模型，确保即使在参与方任意偏离协议的情况下，系统仍能保证数据的隐私性与计算的正确性。

**真实嵌入式设备部署。** 本文的实验系统基于Python多进程仿真，尚未在真实的可穿戴设备上运行。未来的工作可将客户端模块移植至树莓派（Raspberry Pi）或ESP32等嵌入式平台，评估定点数编码、秘密分享生成与TLS通信在资源受限环境下的实际性能与能耗表现。特别是，ESP32等低功耗微控制器的内存与计算资源极为有限，如何在KB级内存中实现32位整数环上的安全计算，是一个具有挑战性的工程问题。

**复杂算法的MPC适配。** 本文选用阈值法作为检测基线，主要考量其MPC友好性。然而，深度学习模型（如轻量化CNN、LSTM）在跌倒检测精度方面通常优于阈值法。未来的研究可探索卷积运算、循环运算与激活函数的安全计算协议，构建面向深度学习推理的高效MPC框架，在提升检测精度的同时保护传感器数据隐私。

**多方场景扩展。** 本文聚焦两方架构，适用于单用户与两台云服务器的场景。在养老院、社区健康中心等多人监测场景中，可能需要同时处理数十名用户的传感器数据。未来的研究可将两方协议扩展至多方（$n > 2$）场景，结合阈值密码（Threshold Cryptography）技术，实现多人数据的联合隐私保护分析，同时保证系统的可扩展性与容错能力。

---

## 参考文献

[1] Kangas M, Konttila A, Winblad I, et al. Comparison of low-complexity fall detection algorithms for body attached accelerometers[J]. Gait & Posture, 2008, 28(2): 285-291.

[2] Bourke A K, O'Brien J V, Lyons G M. Evaluation of a threshold-based tri-axial accelerometer fall detection algorithm[J]. Gait & Posture, 2007, 26(2): 194-199.

[3] Mainali P, Shepherd C. Privacy-enhancing fall detection from remote sensor data using multi-party computation[C]//Proceedings of the 14th International Conference on Availability, Reliability and Security (ARES). New York: ACM, 2019: 1-10.

[4] Yao A C. How to generate and exchange secrets[C]//Proceedings of the 27th Annual Symposium on Foundations of Computer Science (FOCS). Washington: IEEE, 1986: 162-167.

[5] Goldreich O, Micali S, Wigderson A. How to play any mental game[C]//Proceedings of the 19th Annual ACM Symposium on Theory of Computing (STOC). New York: ACM, 1987: 218-229.

[6] Beaver D. Efficient multiparty protocols using circuit randomization[C]//Advances in Cryptology—CRYPTO'91. Berlin: Springer, 1991: 420-432.

[7] Damgård I, Keller M, Larraia E, et al. Practical covertly secure MPC for dishonest majority—or: breaking the SPDZ limits[C]//European Symposium on Research in Computer Security (ESORICS). Berlin: Springer, 2013: 1-18.

[8] Demmler D, Schneider T, Zohner M. ABY—a framework for efficient mixed-protocol secure two-party computation[C]//Proceedings of the 22nd Annual Network and Distributed System Security Symposium (NDSS). San Diego: Internet Society, 2015.

[9] Cramer R, Damgård I, Nielsen J B. Secure multiparty computation and secret sharing[M]. Cambridge: Cambridge University Press, 2015.

[10] Larner A J. An elderly person's fall should not be called a "mechanical fall"[J]. Postgraduate Medical Journal, 2020, 96(1132): 72.

[11] Sadek I, Boukhechba K, Alshurafa N, et al. An effective approach for human fall detection using skeletal data from depth sensor[C]//Proceedings of the 13th International Conference on Sensor Technologies and Applications (SENSORCOMM). Nice: IARIA, 2019: 1-6.

[12] Delahoz Y S, Labrador M A. Survey on fall detection and fall prevention using wearable and external sensors[J]. Sensors, 2014, 14(10): 19806-19842.

[13] Igual R, Medrano C, Plaza I. A comparison of public datasets for acceleration-based fall detection[J]. Medical Engineering & Physics, 2015, 37(9): 870-878.

[14] 中国信息通信研究院. 隐私计算技术金融应用研究报告[R]. 北京: 中国信息通信研究院, 2021.

[15] 国家市场监督管理总局, 国家标准化管理委员会. 信息安全技术 个人信息安全规范: GB/T 35273-2020[S]. 北京: 中国标准出版社, 2020.

[16] Rabin M O. How to exchange secrets with oblivious transfer[R]. Technical Report TR-81, Aiken Computation Laboratory, Harvard University, 1981.

[17] Ben-Or M, Goldwasser S, Wigderson A. Completeness theorems for non-cryptographic fault-tolerant distributed computation[C]//Proceedings of the 20th Annual ACM Symposium on Theory of Computing (STOC). New York: ACM, 1988: 1-10.

[18] Gentry C. Fully homomorphic encryption using ideal lattices[C]//Proceedings of the 41st Annual ACM Symposium on Theory of Computing (STOC). New York: ACM, 2009: 169-178.

[19] McMahan H B, Moore E, Ramage D, et al. Communication-efficient learning of deep networks from decentralized data[C]//Proceedings of the 20th International Conference on Artificial Intelligence and Statistics (AISTATS). Fort Lauderdale: PMLR, 2017: 1273-1282.

[20] Damgård I, Pastro V, Smart N, et al. Multiparty computation from somewhat homomorphic encryption[C]//Advances in Cryptology—CRYPTO 2012. Berlin: Springer, 2012: 643-662.
